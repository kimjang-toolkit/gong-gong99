interface ToastProps {
    leftIcon?: React.ReactNode;
    message: string;
    duration?: number;
}
export default function Toast({ leftIcon, message, duration }: ToastProps): import("react/jsx-runtime").JSX.Element;
export {};
